/** How long (ms) a top-of-panel error banner stays visible before it
 *  auto-dismisses. Shared by the chat view (App) and the Configuration page,
 *  whose error banners would otherwise linger until the user clicks ✕ — or,
 *  on the Configuration page, forever (it has no dismiss control). */
export const ERROR_AUTO_DISMISS_MS = 8000;
